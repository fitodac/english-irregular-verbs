/** @type {import('next').NextConfig} */
const nextConfig = {
	// output: 'export',
	// trailingSlash: true,
	// crossOrigin: 'anonymous',
	// basePath: '/out',
	// distDir: 'dist',
}

export default nextConfig
